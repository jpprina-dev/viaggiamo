/**
 * E2E scenario definitions for driver request decisions.
 *
 * NOTE: Framework-neutral placeholder for current project test stack.
 */

export const driverRequestDecisionScenarios = [
  'driver views pending requests for owned trip',
  'driver accepts a pending request and sees updated status',
  'driver rejects a pending request and sees updated status',
]
